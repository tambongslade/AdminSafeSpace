<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manage Users</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
</head>
<body class="flex">
  <%- include('components/navbar.ejs') %>
  <div class="flex-1 p-6 bg-gray-100 overflow-y-auto">
    <div class="bg-white p-6 shadow-lg">
      <h1 class="text-3xl font-bold">Manage Users</h1>
      <a href="/add_user" class="mt-4 bg-blue-500 text-white px-4 py-2 rounded-full inline-block">Add User</a>
    </div>
    <div class="bg-white mt-6 p-6 shadow-lg rounded-md">
      <table class="w-full">
        <thead class="bg-gray-100">
          <tr>
            <th class="text-left p-4">IMAGE</th>
            <th class="text-left p-4">NAME</th>
            <th class="text-left p-4">EMAIL</th>
            <th class="text-left p-4">ROLE</th>
            <th class="text-left p-4">ACTIONS</th>
          </tr>
        </thead>
        <tbody>
          <% users.forEach(user => { %>
            <tr class="bg-white border-b">
              <td class="p-4"><img src="/uploads/<%= user.image %>" alt="<%= user.name %>" class="w-16 h-16 object-cover"></td>
              <td class="p-4"><%= user.name %></td>
              <td class="p-4"><%= user.email %></td>
              <td class="p-4"><%= user.role %></td>
              <td class="p-4 flex space-x-2">
                <a href="/edit_user/<%= user.id %>" class="text-blue-500">✏️</a>
                <form action="/delete_user/<%= user.id %>" method="POST" class="inline">
                  <button type="submit" class="text-red-500">🗑️</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>
</body>
</html>
